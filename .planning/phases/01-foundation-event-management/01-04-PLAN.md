---
phase: 01-foundation-event-management
plan: "04"
type: execute
wave: 3
depends_on:
  - "00"
  - "03"
files_modified:
  - buddyboss-events/src/bp-events/bp-events-filters.php
  - buddyboss-events/src/bp-events/classes/class-bp-events-list-table.php
autonomous: true
requirements:
  - ADMN-01
  - ADMN-02

must_haves:
  truths:
    - "When moderation is enabled, creating an event via bp_events_create_event() sets status=pending (not published)"
    - "Admin clicking Approve on a pending event in the list table changes its status to published"
    - "bp_events_user_can_create() returns false for non-admin users when creation permission is set to admins-only"
    - "bp_events_user_can_create() returns true for any logged-in user when permission is set to members"
  artifacts:
    - path: "buddyboss-events/src/bp-events/bp-events-filters.php"
      provides: "wp_ajax_bp_events_approve handler + moderation status filter"
    - path: "buddyboss-events/src/bp-events/classes/class-bp-events-list-table.php"
      provides: "Approve row action linked to the AJAX handler"
  key_links:
    - from: "BP_Events_List_Table Approve link"
      to: "wp_ajax_bp_events_approve"
      via: "admin-ajax.php POST with event_id and nonce"
      pattern: "wp_ajax_bp_events_approve"
    - from: "bp_events_create_event()"
      to: "bb_events_moderation_enabled option"
      via: "filter applied inside create function"
      pattern: "bb_events_moderation_enabled"
---

<objective>
Wire the admin approve action for pending events (ADMN-02) and verify that creation-permission enforcement (ADMN-01) already works correctly via bp_events_user_can_create().

Purpose: ADMN-01's permission function already reads the bb_events_creation_permission option and enforces it — the tests validate this works. ADMN-02 is the gap: the Approve link is rendered in the list table but has no handler. This plan closes that gap.

Output: AJAX approve handler registered in bp-events-filters.php + list table Approve link wired with correct nonce and JS confirmation.
</objective>

<execution_context>
@/Users/tom/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tom/Local Sites/Events/.planning/ROADMAP.md
@/Users/tom/Local Sites/Events/.planning/phases/01-foundation-event-management/01-CONTEXT.md
@/Users/tom/Local Sites/Events/.planning/phases/01-foundation-event-management/01-RESEARCH.md

<interfaces>
<!-- From class-bp-events-list-table.php (existing) -->
// column_title() renders row actions including Approve link
// Approve link currently: <a href="?page=bp-events&action=approve&event_id={id}">Approve</a>
// Needs to change to: POST to admin-ajax.php with action=bp_events_approve, event_id, nonce

<!-- From bp-events-admin.php (existing) -->
// bp_events_user_can_create($user_id, $group_id = null) reads bb_events_creation_permission
// Returns bool. Values: 'admins' | 'organizers' | 'members'
// Already functional per RESEARCH.md — tests verify it, no changes needed to the function itself.

<!-- From bp-events-functions.php (existing) -->
```php
function bp_events_create_event( $args = array() ) {
    // When bb_events_moderation_enabled is true, should set status='pending' even if 'published' was requested
    // Check if this is already enforced or needs a filter
}
```

<!-- Admin approve AJAX pattern (from RESEARCH.md) -->
```php
add_action( 'wp_ajax_bp_events_approve', function() {
    check_ajax_referer( 'bp_events_admin_action', 'nonce' );
    if ( ! current_user_can( 'manage_options' ) ) {
        wp_send_json_error( __( 'Permission denied.', 'buddyboss' ) );
    }
    $event_id = absint( $_POST['event_id'] );
    $result = bp_events_update_event( $event_id, array( 'status' => 'published' ) );
    if ( $result ) {
        wp_send_json_success();
    } else {
        wp_send_json_error( __( 'Could not approve event.', 'buddyboss' ) );
    }
} );
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire approve AJAX handler and moderation status enforcement</name>
  <files>
    buddyboss-events/src/bp-events/bp-events-filters.php
    buddyboss-events/src/bp-events/classes/class-bp-events-list-table.php
  </files>
  <behavior>
    - test_approve_pending_event: Given an event with status='pending', calling bp_events_update_event($id, ['status' => 'published']) returns true and bp_events_get_event($id)->status === 'published'.
    - test_creation_permission_admins_only: When bb_events_creation_permission option is 'admins', bp_events_user_can_create($non_admin_user_id) returns false.
    - test_creation_permission_members: When bb_events_creation_permission is 'members', bp_events_user_can_create($any_logged_in_user_id) returns true.
  </behavior>
  <action>
    In bp-events-filters.php:

    1. Register the approve AJAX handler exactly as shown in the research code example. Use check_ajax_referer('bp_events_admin_action', 'nonce'). Verify current_user_can('manage_options'). Use absint() on event_id. Call bp_events_update_event() to set status='published'. wp_send_json_success() on success, wp_send_json_error() on failure.

    2. Add a filter on 'bp_events_before_save' (or the equivalent hook inside bp_events_create_event — check bp-events-functions.php for what hook fires before status is saved): when bb_events_moderation_enabled option is truthy AND the requested status is 'published' AND $event->parent_event_id is null (don't moderate child occurrences), override the status to 'pending'. Occurrence child rows (parent_event_id set) are always published without moderation.

    In class-bp-events-list-table.php:

    Update the Approve row action in column_title() to use an AJAX pattern instead of a GET link:
    - Change the Approve link href to '#'
    - Add data attributes: data-event-id="{id}" data-nonce="{wp_create_nonce('bp_events_admin_action')}" data-action="bp_events_approve"
    - Add class "bp-events-approve-btn" for JS targeting
    - Add a wp_localize_script or inline script (only when on the events list page) with the admin-ajax URL

    Add a small inline JS block in the list table's display_rows() or a separate enqueue call (only on this admin page): listen for click on .bp-events-approve-btn, POST to ajaxurl with the data attributes, on success reload the page or update the row's status cell.

    Inline JS can use jQuery if available (WordPress admin always has jQuery). Follow WordPress JS Coding Standards: var declarations, jQuery( document ).on( 'click', ... pattern.
  </action>
  <verify>
    <automated>cd "/Users/tom/Local Sites/Events/buddyboss-events" && vendor/bin/phpunit tests/phpunit/testcases/test-admin.php tests/phpunit/testcases/test-permissions.php -x 2>&1 | tail -10</automated>
  </verify>
  <done>wp_ajax_bp_events_approve handler exists in bp-events-filters.php. Approve link in list table uses data attributes for AJAX. Moderation status override filter exists. test-admin.php and test-permissions.php run without fatals.</done>
</task>

</tasks>

<verification>
- `php -l buddyboss-events/src/bp-events/bp-events-filters.php` — no errors
- `php -l buddyboss-events/src/bp-events/classes/class-bp-events-list-table.php` — no errors
- `vendor/bin/phpunit tests/phpunit/testcases/test-admin.php tests/phpunit/testcases/test-permissions.php` — no fatals
- grep confirms 'wp_ajax_bp_events_approve' is registered in filters file
</verification>

<success_criteria>
- AJAX approve handler registered and uses check_ajax_referer()
- Moderation status override filter prevents 'published' status when moderation is enabled
- List table Approve link uses AJAX (not a GET redirect)
- All modified files pass php -l
</success_criteria>

<output>
After completion, create /Users/tom/Local Sites/Events/.planning/phases/01-foundation-event-management/01-04-SUMMARY.md
</output>
