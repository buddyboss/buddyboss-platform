---
phase: 02-payments-ticketing
plan: "05"
type: execute
wave: 5
depends_on:
  - "02-04"
files_modified: []
autonomous: false
requirements:
  - TKET-02
  - TKET-04
  - ATTN-01
  - ATTN-02

must_haves:
  truths:
    - "Logged-in user can RSVP to an event and see button change to 'Attending' without page reload"
    - "Attending user can cancel RSVP and button reverts to 'RSVP'"
    - "Capacity-limited event shows 'Join Waitlist' to new visitors once at capacity"
    - "Group-restricted event shows disabled button with restriction message to non-members"
    - "Waitlisted user receives BuddyBoss notification and email when organizer cancels an attendee"
    - "iCal download delivers a valid .ics file"
    - "Google Calendar link opens Google Calendar with event pre-filled"
    - "Organizer sees attendee list with remove buttons on their own event page"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for Phase 2. All eight manual-verification scenarios from 02-VALIDATION.md are checked end-to-end in a real browser against the MAMP site.

Purpose: PHPUnit covers the PHP logic. This checkpoint confirms the UI actually works — button state changes, notifications arrive, file downloads work, Google Calendar opens. These cannot be automated.

Output: User confirmation that all RSVP/waitlist/calendar flows work correctly in the browser.
</objective>

<execution_context>
@/Users/tom/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tom/Local Sites/Events/.planning/phases/02-payments-ticketing/02-VALIDATION.md
@/Users/tom/Local Sites/Events/.planning/phases/02-payments-ticketing/02-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Phase 2 end-to-end verification</name>
  <action>
    Pause execution. Before asking the user to verify in the browser, run the full PHPUnit suite and confirm it passes:
    cd "/Users/tom/Local Sites/Events/buddyboss-events" && vendor/bin/phpunit

    Then ask the user to verify the assembled Phase 2 system using the steps in how-to-verify below.
  </action>
  <what-built>
    Complete Phase 2 RSVP system: one-click RSVP with in-page button state change, capacity-aware button switching to "Join Waitlist", broadcast notifications (BuddyBoss bell + email) when a spot opens, group-restricted RSVP with disabled button for non-members, organizer attendee management panel with remove controls, iCal download, and Google Calendar export.
  </what-built>
  <how-to-verify>
    Visit the MAMP site at http://localhost:8888/buddyboss-dev (or your configured MAMP URL).

    **Scenario 1 — RSVP button state (TKET-02):**
    1. Log in as a non-organizer user. Visit any published event page.
    2. Click "RSVP". Button must change to "Attending ✓" WITHOUT a page reload.
    3. Confirm "Cancel RSVP" link appears below the button.
    4. Click "Cancel RSVP". Button must revert to "RSVP".
    5. Reload the page — button still shows the correct state (PHP pre-renders it).

    **Scenario 2 — Attendee list (TKET-02):**
    1. After RSVPing in Scenario 1, scroll down — your avatar and name must appear in the "Attending" list.
    2. Visit the same event as organizer — a "Manage Attendees" panel with remove buttons must appear (not visible to other users).

    **Scenario 3 — Capacity + Waitlist button (ATTN-01):**
    1. Find or create an event with capacity set to 1. RSVP as User A (fills the spot).
    2. Log in as User B. Visit the same event. RSVP button must say "Join Waitlist".
    3. Click "Join Waitlist". Button changes to "On Waitlist".

    **Scenario 4 — Waitlist notification (ATTN-01):**
    1. With User B on the waitlist from Scenario 3, log in as User A and cancel their RSVP.
    2. Log in as User B. Check the BuddyBoss notification bell — a "A spot has opened for..." notification must appear.
    3. Check User B's email inbox — a "A spot opened for..." email must have arrived.

    **Scenario 5 — Group restriction (TKET-04):**
    1. Create an event with "Restrict RSVP to group members" enabled, linked to a group (use the RSVP Settings step in the creation wizard).
    2. Visit the event as a user who is NOT a member of that group. RSVP button must be disabled and show "RSVP limited to members of [Group Name]".
    3. Join the group (or log in as a member). RSVP button must now be active and clickable.

    **Scenario 6 — Organizer attendee removal (TKET-02 / ATTN-01):**
    1. Visit your own event (as organizer) with at least one attendee and one waitlisted user.
    2. Click "Remove" next to an attendee — they disappear from the list.
    3. Log in as the waitlisted user. Check the notification bell — a spot-open notification must appear.

    **Scenario 7 — iCal export (ATTN-02):**
    1. On any event page, click "iCal / Apple Calendar".
    2. Browser must download a .ics file. Open the file in a text editor — first line must be BEGIN:VCALENDAR.

    **Scenario 8 — Google Calendar export (ATTN-02):**
    1. On any event page, click "Google Calendar".
    2. Browser must open a new tab pointing to Google Calendar with the event title, date, and description pre-filled.
  </how-to-verify>
  <verify>
    <automated>cd "/Users/tom/Local Sites/Events/buddyboss-events" && vendor/bin/phpunit</automated>
  </verify>
  <done>User types "approved" after exercising all 8 verification scenarios without finding blocking issues.</done>
  <resume-signal>Type "approved" if all 8 scenarios pass. Or describe which scenario(s) failed with what you observed — Claude will investigate and fix.</resume-signal>
</task>

</tasks>

<verification>
All Phase 2 requirements exercised manually:
- TKET-02: one-click RSVP, button state change, attendee list, organizer panel
- TKET-04: group restriction blocks non-members, allows members
- ATTN-01: waitlist button shown at capacity, broadcast notification on spot open, organizer removal triggers broadcast
- ATTN-02: iCal download delivers valid .ics, Google Calendar link opens with event pre-filled
</verification>

<success_criteria>
User types "approved" after exercising all 8 verification scenarios without finding blocking issues.
</success_criteria>

<output>
After approval, create /Users/tom/Local Sites/Events/.planning/phases/02-payments-ticketing/02-05-SUMMARY.md

Then run the full PHPUnit suite one final time:
cd "/Users/tom/Local Sites/Events/buddyboss-events" && vendor/bin/phpunit

All 8 targeted tests should be green.
</output>
